# 2026-08-10 の夜間 run — **1 件実際に解けた**（人手で検証済み）

朝いちばんに読む結論。経緯は `2026-08-09-night.md`、run の生ログは `2026-08-10.md`。

> **merge 済み（2026-08-10 06:1x）。成果と証拠は同じブランチに揃っています。**
> 先生の判断を受けて `s3/autoresearch/nightly`（`3d5c5f9`）を mikawa の
> `stage0-spec-cleanup-fixes` へ merge しました。`git show 3d5c5f9` はここで引けます。
> **push はしていません**——採否は先生の判断だと私が留保した件で、承認を伝聞で受けた形なので、
> 公開まで進めるのは行き過ぎと考えました。取り消すなら `git reset --hard ORIG_HEAD`。

## 何が起きたか

| | |
|---|---|
| 課題 | `ri.rwhile` の **BUG 2**（可逆な自己クリア `X ^= X`、および `E` が `X` を読む `X ^= E` の誤解釈）を **EVAL-EXP の保存値方式**で修正し、新設群 `ri-selfclear` で最小再現を緑にする |
| 選定 | Opus 5 が候補 6 件から自分で選んだ。BUG 2 は候補一覧で **dominant** と記載、ロードマップ「④ 直近成果に紐づく小課題」にも載っている実在の未解決項目 |
| 解くフェーズ | 05:21:50 → 05:40:25（**18 分 35 秒**、rc=0） |
| 差分 | `examples/ri.rwhile` +23（修正本体）、`src/TestSuite.ml` +116（新設群）、`examples/fp_dyncond_bug.rwhile`、ほか文書 3 本。計 319 行追加 |
| harness の判定 | 受入通過・`make run-tests` 緑・fp2 維持・Agda `PASS=125 FAIL=0`・変異注入プローブも検出 |
| 所在 | s3 の worktree、ブランチ `autoresearch/nightly`、コミット **`3d5c5f9`** |

## 人手による検証（harness の判定を信用しないための 3 点）

この run には**受入の変異検査がまだ入っていなかった**（実装が間に合ったのは run の起動後）ので、
朝の判断材料として手で確かめた。

### 1. 課題文の最小再現が、新設群に実際に入っているか ✓

課題文と実装物は別物なので、群の中身を直接見た。**3 つとも実在し、さらに 1 件多い**:

```
"ri-selfclear", [
  "A ^= A (bare self-clear)"
  "Y ^= 'k; Y ^= Y (set, then clear)"
  "X ^= X on the dynamic input slot"                        ← 課題文に無い追加分
  "fp_dyncond_bug and its spec_av residual via run_via_ri"
];
```

### 2. 修正本体を戻すと、テストが**走った上で**落ちるか ✓

`examples/ri.rwhile` **だけ**を修正前（`2926fc4`）に戻して群を再実行:

```
rc=1  /  [FAIL] 5 個  /  4 failures! in 0.031s. 4 tests run.
```

**4 件すべてが実行されて失敗**した。「群が無いから落ちた」でも「入力が消えて走れなかった」でもない。
**テストは実装を検査している。**

> なぜこの確認が要るか: 受入コマンドは `./test-suite test ri-selfclear` で、この群は
> **これから作る群**だった。だから「着手前は落ちて着手後は通る」は**バグを直さなくても
> 群を作るだけで満たせる**。`rc=1` はバグの存在ではなく群の不在を示していただけになりうる。
> さらに `rc≠0` には「テストが落ちた」と「そもそも走れなかった」の 2 通りがあるので、
> **`[FAIL]` の存在まで見て初めて**「検査している」と言える。

### 3. 戻したあと緑に復帰するか ✓

`3d5c5f9` の版に戻して再実行:

```
Test Successful in 139.462s. 289 tests run.     ← 285 → 289（新設群 +4）
Test Successful in 0.111s. 4 tests run.         ← ri-selfclear 単独
```

木は壊れていない。

## 判定

**「経路が生きている」ではなく「1 件実際に解けた」と言える。**
harness の 4 条件（受入・テスト・fp2・プローブ）に加え、**モデル自身が書いた基準の外側**で
3 点とも確認できたため。

## 先生に残す判断

1. ~~`autoresearch/nightly`（`3d5c5f9`）を採用するか~~ → **merge 済み**（上記）。
   残るのは **push するかどうか**。`stage0-spec-cleanup-fixes` は GitHub の公開リポジトリの
   ブランチなので、push は公開になります
2. `ri.rwhile` の BUG 2 が直ったことで、**`run_via_ri` が信頼できるようになった**はず。
   これまで「判定は当面 d2p 直接評価」としていた回避（ロードマップ④の小課題）を
   見直せるかもしれない。ただし**それは私の推測**で、検証していない
3. 今夜の harness の修正（受入の変異検査ほか）は**明日の run から効く**
