# R-WHILE-S — 表層言語と、R-WHILE への最適化コンパイラ

**R-WHILE-S**（S = Sugar）は、書きやすさと実行効率のための構文を載せた表層言語。
**R-WHILE の核は凍結する。** 自己解釈の容易さは核の小ささそのものであり、解釈
オーバヘッド定数 `a_p` も `proofs/agda/` の 27 モジュールも核の構文に張り付いて
いる。核に構文を足すと `examples/ri.rwhile`・`examples/spec_av.rwhile`・形式化の
三つ全部に波及する。

```
R-WHILE-S  --(脱糖 Desugar.ml)-->  R-WHILE  --(最適化 Optimize.ml)-->  R-WHILE
                                      |
                                      +--> p2d --> ri.rwhile / spec_av.rwhile
                                      +--> proofs/agda/（核のまま）
```

## 「両取り」はどこまで成立するか

コンパイラは構造を消すので、**ノード数を減らす種類の利得は自己解釈の側へ渡らない**。
最適化は 2 種類に分かれ、扱いが違う。

| 最適化 | コンパイル後も残るか | 実測の根拠（2026-08-06、本機） |
|---|---|---|
| **変数の採番順**（パス 1・実装済み） | **残る**（出力プログラムの採番が変わる） | 実例で SI 歩数 2〜10% 減 |
| **スロット共有**（パス 2・実装済み） | **残る**（出力の変数が実際に減る） | `spec` で 155→119 スロット（23%）。1 個 = 20 SI 歩 |
| **静的変数を最後に採番**（パス 3・実装済み） | **残る**（残余が小さくなる） | fp1 残余 103→87 ノード（0.63x→0.51x） |
| 死コード除去・ループ不変移動 | **残る** | 古典的最適化はすべてこちら |
| n 項ブロック | **消える**（`;` に戻る） | 直線 n 命令 = `2n−1` 歩、約半分が `;` |
| push/pop プリミティブ | **消える**（`<=` に戻る） | パターン走査 約 53/267 歩 |

上半分は **`ri.rwhile` と `spec_av.rwhile` を無改変のまま**効かせられる。下半分は
「S を直接実行するとき」にだけ効き、自己解釈に効かせるには核を広げるしかない。

## コンパイラのパス

### パス 1: アクセス重みによる採番（実装済み・`src/Optimize.ml`）

`program2data` は変数を **初出順**で 1..n に採番する。自己解釈系のストアはその番号
で索引する連結リストなので、変数 i の参照はセルを i 個たどる（DLOOKUP/DUPDATE）。
**熱い変数を前に置けば、その走査がすべて短くなる。**

重みは古典的な静的ヒューリスティック — 出現 1 回につき 1 点、囲むループごとに
10 倍（繰り返し実行されるのはそこだから）。条件分岐では倍率を変えない。同点は
初出順を保つので決定的。

**2026-08-06 から既定。** 旧採番は `./ri -p2d -first-occurrence-vars` で戻せる。

```bash
./ri -p2d prog.rwhile                        # 重み順（既定）
./ri -p2d -first-occurrence-vars prog.rwhile # 初出順（旧）
```

**実測（`examples/ri.rwhile` で自己解釈したときの歩数）**

| プログラム | 変数数 | 既定 | `-hot-vars` | 改善 |
|---|---:|---:|---:|---:|
| `reverse` | 3 | 3773 | 3673 | 2% |
| `rle` | 6 | 8030 | 7798 | 2% |
| `length` | 4 | 8761 | 8317 | 5% |
| `minus` | 4 | 5011 | 4731 | 5% |
| `compare` | 8 | 18027 | 16137 | **10%** |

変数が多いほど効く。純粋な改名なので**答は変わらない**（5 例すべてで直接評価と
一致することを確認、`compiler` テスト群で固定）。

**より大きな効果（後から判明）: 符号化サイズが半分になる。** 変数番号は p2d では
**単進 numeral** で書かれるので、変数 i の出現 1 つが O(i) ノードを食う。出現回数の
降順に並べることは「頻度 × 符号長の総和」を最小化することであり、符号長が番号に
単調なので**この並べ方が最適**である。fp2 のコンパイラは符号化を抱えた残余
プログラムなので、一緒に縮む。

| 指標 | 既定 | `-hot-vars` | 削減 |
|---|---:|---:|---:|
| `spec_av` の符号化 | 817701 ノード | 399565 | **51%** |
| comp2 = `[spec_av]((spec_av.ri_min))` | 812515 ノード | 386681 | **52%** |
| comp2 のループ本体内ノード | 1157729 | 353665 | **69%** |

`[comp2](('S.swap)) == B` は成立したまま。`loop_weight` を 1 / 10 / 100 と振っても
符号化サイズは同一だったので、ヒューリスティックは繊細ではない。

**既定にした根拠（2026-08-06）**: `RWHILE_HOT_VARS=1` で全スイートを回すと、
落ちたのは**採番に依存した期待値 3 件だけ**で、実際の退行はゼロだった。しかも
**スイート全体が 125 秒 → 36 秒（3.5 倍）**になる。fp1・fp2・fp3 はいずれも成立。

切り替えに伴って直した 3 件:

| 落ちたもの | 実態 | 対応 |
|---|---|---|
| `compiler`: 既定 OFF の検査 | 設計どおり | 既定 ON を検査するテストに書き換え、旧採番も `reverse_first_occurrence.val` で固定 |
| `file-integration`: `reverse.val` | 期待値ファイル | 再生成。旧版は `reverse_first_occurrence.val` に保存 |
| `assemble-fp1`: 既知バグの特徴づけ | **変数番号 0 を固定していた**（バグは健在、番号が 36 に動いただけ） | 番号ではなく「不透明な `('D . ('var . N))` である」という**形**を主張するよう修正。テストが採番を追いかけていた |

### パス 2: スロット共有（実装済み・`./ri -p2d -share-slots`）

生存区間が互いに素な変数に**同じストアスロット**を割り当てる（線形走査レジスタ
割り当て）。

**なぜ効くか（実測 2026-08-06）**: 本文の長さを固定して変数の数だけ変えると、
自己解釈のコストは変数数 n について `387 + 20n`。一方、**同じ変数を再利用する
未実行の命令を 70 個足してもコストはゼロ**だった。課金されるのはストアの
**長さ**であってプログラムの大きさではない。

| k | 同じ変数を再利用 | 毎回別の変数 |
|---:|---|---|
| 10 | 変数 4 → 407 | 変数 13 → 587 |
| 40 | 変数 4 → **407** | 変数 43 → 1187 |
| 80 | 変数 4 → **407** | 変数 83 → 1987 |

**健全性は R-WHILE 自身の不変条件から出る。** 全変数は開始時 nil、終了時 nil
（`all_cleared`）で、値が変わるのは自分の出現箇所だけ。ゆえに変数は**最初の出現
より前と最後の出現より後は nil** であり、出現区間が互いに素な 2 変数が同時に
非 nil になることはない。

補正は 2 つ:

- **ループ**: ある反復で設定し次の反復で消す変数は、区間の外でも非 nil。よって
  ループに触れる区間はそのループ全体まで広げる（入れ子のため不動点まで）。
- **条件分岐は補正不要で、しかもここが稼ぎどころ**。両腕は排他なので then 腕
  だけのスクラッチと else 腕だけのスクラッチは同時に生きない。両腕にまたがる
  変数は区間が両方を覆うので自動的に除外される。

区間の集合は区間グラフなので、開始位置順の貪欲彩色が最適（色数＝同時生存数）。

**実測（スロット数）**

| プログラム | 変数数 | スロット | 削減 |
|---|---:|---:|---:|
| `spec` | 155 | 119 | **23%** |
| `spec_av_rev` | 244 | 209 | 14% |
| `spec_av` | 230 | 204 | 11% |
| `ri` / `compare` / `reverse` | — | 同数 | 0% |

分岐主体の特殊化器で効き、ループ主体の小さな例では**正しく**何も共有しない。
スロット 1 個 = 20 SI 歩なので `spec` は 1 実行あたり約 720 歩の節約。さらに
`specsize` がストアを小さく生成できるので fp2/fp3 全体が速くなる。

**パス 1 と合成すること。** 割り当て順にスロット番号を振ると熱い変数が高い番号に
落ちて、スロットが減っているのに**遅くなる**（実測で `minus` が 7%、
`lookup_ppl2015` が 13% 悪化した）。スロットを共有する変数の重み合計で番号を
振り直すと解消し、7 例すべてで既定以上になる。

### パス 2 を特殊化器に食わせてはいけない

上の健全性の議論は**実行**についてのものである。`spec_av` は被験プログラムを実行
せず、注釈付き値を伝播させる。その AV ストアは**変数番号ごとに 1 つ**の束縛時間
情報を持つので、2 変数を併合すると注釈が併合されてしまい、特殊化としては不健全に
なる。

実測:

| | fp1 残余 (swap / id) | fp2 (comp2) |
|---|---|---|
| 既定 | 103 / 63 ノード | 812515 ノード ✓ |
| `-hot-vars` | 103 / 63 | **386681** ✓ |
| `-share-slots` | **91 / 39**（往復も成立） | **失敗**: `Pattern matching failed: '41 and 'error are not equal (in inv_evalPat)` |

fp1 では動くどころか残余が小さくなるが、fp2 で落ちる。**パス 1 だけなら安全で、
しかも利得のほとんどはそちらにある。** `-share-slots` は自己解釈と直接実行の
経路に限って使うこと。

### パス 3: 静的変数を最後に採番（実装済み・`./ri -p2d -static-vars X,Y`）

パス 1 は**ソースの符号化**を最小化する。特殊化の**残余**は別の目的関数であり、
どれくらい別かを `ri_min` の全 120 順列で測った:

- swap の fp1 残余は **83〜115 ノード**、id は **39〜79 ノード** — 採番だけで決まる
- しかも**最良の採番はソース符号化が大きい**（171 対 163 ノード）

関係は正確に線形だった。唯一の静的変数 `Op` の位置ごとに平均を取ると:

| `Op` の位置 | 1 | 2 | 3 | 4 | 5 |
|---|---:|---:|---:|---:|---:|
| swap 残余の平均 | 109.0 | 104.0 | 99.0 | 94.0 | 89.0 |

**1 つ後ろにずらすごとに正確に 5 ノード。** 理由は明快で、残余に生き残るのは
**動的**変数だけだから。静的変数を後ろに送ると動的変数の番号がすべて 1 つ下がり、
番号は単進 numeral なので残余中の動的変数の出現ごとに cons が 1 つ減る。従って
規則はパス 1 と同じ頻度の議論を、**ソースではなく残余に**当てはめたものになる:
生き残る変数に小さい番号を与えよ。

残余は特殊化する前には分からないが、**束縛時間の分割は特殊化の入力**なので、
静的変数を呼び出し側が指定すればよい。

```bash
RWHILE_STATIC_VARS=Op ./measure_proj     # または ./ri -p2d -static-vars Op
```

**実測**（`Op` を静的と宣言）:

| | 既定 | パス 3 | 全順列の最適 |
|---|---:|---:|---:|
| swap 残余 | 103（0.63x） | **87（0.51x）** | 83 |
| id 残余 | 63 | **55** | 39 |

往復も swap の意味も保たれる。最適値に届かないのは**動的変数どうしの順序**も
効くためで、そこは残余を見ないと決められない。

### 効用の減衰（2026-08-06 の実測。ここで打ち止めにすべき）

3 つのパスを既定（パス 1）に対して測り直すと、**利得のほぼ全部がパス 1 に集中**して
いる。

| 設定 | ソース符号化 | fp1 swap | fp1 id | fp2 |
|---|---:|---:|---:|---|
| 旧採番（パスなし） | 817701 | 103 | 63 | 812515 ✓ |
| **パス 1（既定）** | **399565** | 103 | 63 | **386681** ✓ |
| + パス 2 | 371853 | 91 | **39** | **失敗** |
| + パス 3 | 399565 | **87** | 55 | ✓（fp1 のみ検証） |
| パス 2 + パス 3 | 371853 | 91 | 39 | 失敗 |
| （全順列の最適） | — | 83 | 39 | — |

読み取れること:

- **パス 1 だけが広く効く**。符号化 −51%、fp2 コンパイラ −52%、テストスイート
  125 秒 → 36 秒（3.5 倍）。しかも fp1 残余は変えない（103/63 のまま）ので、
  効いているのは純粋に符号長である。
- ~~**パス 2 の実現利得はほぼゼロ**~~ **← 誤り。2026-08-06 に訂正。** 測ったのが
  たまたまループ主体の 7 本だけだった。**リポジトリの全例で測り直すと 16 本で
  スロットが減る**（`sugar` 8→5 = 37%、`av` 45→29 = 35%、`reverse_step` 3→2 = 33%、
  `spec` 155→119 = 23%、`fp_nested_read` 7→5 = 28% ほか）。自己解釈の歩数でも
  **`sugar.rwhile` が 9192 → 8102 歩（−11%）**、結果は直接評価と一致する。
  利得は実在する。ただし**fp2 を壊す**制限は変わらないので、自己解釈と直接実行の
  経路に限る。
- **パス 3 は狭い**。5 変数の被験 1 本で swap −16%。分割を人手で与える必要があり、
  `ri` や `spec_av` のような大きな被験では未検証。
- **パス 2 と 3 は合成しても改善しない**（91/39 = パス 2 単独）。どの設定も最適
  83/39 に届かない。

つまり**限界効用は減衰している**。ただし上記のとおりパス 2 の評価は 2026-08-06 に
訂正した（「利得ゼロ」ではなく、分岐主体のプログラムで実際に効く）。**(a) パス 2 の
決着は「残す」で完了**。残るのは (b) 動的変数どうしの順序を残余の実測から決める
反復法（原理は上の線形関係で説明済み）、(c) 別種の作業に移ること。

### 教訓（測り方）

「利得ゼロ」と判定したときの標本は**ループ主体の 7 本だけ**だった。スロット共有は
条件分岐の腕どうしで効く最適化なので、ループ主体のプログラムばかり測れば当然何も
出ない。**最適化の効く構造を含む標本で測る**こと。全 40 例超で測り直したら 16 本で
削減が出た。

### 符号化のノードはどこへ行っているか（2026-08-08 の実測。採番の頭打ちと、その先）

「符号化データを縮める」を続ける前に、**ノードの帰属を数えた**（`src/Encoding.ml`、
`./measure_proj encoding`、`full` では comp2 についても出す）。8 つの分類の合計が
`count_nodes` と厳密に一致することを検査してある（`encoding-breakdown` 群 8 件）。
会計が合わない帰属は、無いより悪い（次の最適化を間違った項へ向ける）ため。

| | 全体 | var_index | const | 構造（tag・spine） |
|---|---:|---:|---:|---:|
| `spec_av` | 415733 | **381700 (91.8%)** | 4087 (1.0%) | 17768 (4.3%) |
| `spec` | 218517 | **190727 (87.3%)** | 2133 (1.0%) | 15211 (7.0%) |
| `ri` | 83313 | **62408 (74.9%)** | 2336 (2.8%) | 10979 (13.2%) |
| `comp2` | 416447 | **382654 (91.9%)** | 4227 (1.0%) | 17544 (4.2%) |
| `ri_min` | 163 | 69 (42.3%) | 3 (1.8%) | 51 (31.3%) |

**符号は事実上ぜんぶ単進の変数番号である。** 定数の埋め込みもパターンもタグも、
合計して 5% しかない。したがって「符号化を縮める」＝「変数番号を縮める」に尽きる。
（`~113 ノード/命令` の内訳がこれ。CLAUDE.md の「嵩はパターンと埋め込み定数」は
測る前の推測で、**誤り**だった。パターンは 0.9%、定数は 1.0%。）

てこは 2 つしかなく、両方とも閉じた形で上界が出る。

1. **採番を変える（パス 1・3 の系統）— 残り 9.6% で頭打ち。**
   `var_index = Σ occ(v)·(2·rank(v)+1)` なので、並べ替え不等式より最小値は
   **出現回数の降順に rank を配る**ことで達成される（240! の探索は不要）。
   実測: spec_av は 381700 に対し最小 341952。つまり**どんな採番パスを足しても
   プログラム全体の 9.6% 以上は取れない**。comp2 でも 9.5%。
   なお差が残るのは、パス 1 が**ループ本体を重み付けした動的アクセス回数**で
   並べているから（`Optimize.weight_of` の `loop_weight`）。それは自己解釈の
   **歩数**の目的関数であって符号長のそれではない。両者は両立しないので、
   「符号長最小の採番」を入れるなら歩数の退行を測ってから決めること。

2. **numeral の表現を変える — spec_av で 0.24x。ただし小さい被験では逆効果。**
   ビット列表現（最下位ビットから、`nil`=0・`(nil.nil)`=1、nil 終端）を仮定した
   ときの var_index を実測の番号分布から計算した（`Encoding.binary_var_index`。
   コスト模型は `bits_nodes` として単体で検査済み）。

   | | 現在 | ビット列 | ＋最適採番 | 比 |
   |---|---:|---:|---:|---:|
   | `spec_av` | 415733 | 103277 | **100619** | **0.24x** |
   | `spec` | 218517 | 78057 | 76283 | 0.35x |
   | `ri` | 83313 | 54459 | 49645 | 0.60x |
   | `ri_fp3` | 48821 | 33661 | 33453 | 0.69x |
   | `comp2` (伝播後) | 400531 | 101549 | 98985 | 0.25x |
   | `ri_min` | 163 | 183 | 183 | **1.12x** |

   **`ri_min` は増える。** 5 変数では単進の裾が無く、ビット列の定数費用だけが乗る。
   利得は変数の多さの関数であり、大きな被験でしか出ない。

   **比 comp2/|spec_av| はほとんど動かない**（0.963 → 0.984 とむしろ悪化）。
   両者とも 92% が同じ項なので、同じ率で縮む。この てこ は**絶対値**のためのもので、
   射影の比のためのものではない（比の目標は既に達成済み）。

   代償は測っていない: 番号を**歩く**もの全部（`ri.rwhile`・`ri_fp3.rwhile` の
   ストアアクセス、`spec_av` の AV ストア、`Core.ml`、Agda 核）が単進の減算を
   前提にしている。可逆な二進減算に置き換える必要があり、**歩数は増える公算が
   大きい**（`-steps` は値の大きさを数えないので、上の「計測の落とし穴」にある
   `-work` が先に要る）。着手する前にこの見積りを取ること。

### パス 4 以降（未着手）

死コード除去、ループ不変移動。`local X in C end` は依然 p2d が符号化できない
（`CLocal cannot be translated to data`）。パス 2 が生存区間を自動で求めるので
`local` は必須ではなくなったが、注釈として使えるようにする余地はある。

## ネイティブ S 評価器（未着手）

n 項ブロックと push/pop プリミティブは、脱糖せず `EvalRwhile.evalCom` が直接
実行して初めて効く。核も p2d も触らないので、`./ri` の拡張として独立に足せる。

## コンパイラは導出できる

R-WHILE で書いた R-WHILE-S インタプリタ `rint_S` があれば、コンパイラは第 1
Futamura 射影で得られる — `target = [rspec](rint_S, src)`。2 つ別々に書いて整合性
を保つ必要はない。ただし可逆言語では通常の射影が成立せず `rint_S` も**プログラム
保存**でなければならない（`FUTAMURA.md`）。現状 fp1 が通っているのは `ri_min`
（5 変数）で、完全な S インタプリタへの拡大は別物の規模である。

## 計測の落とし穴

**S の歩数と R-WHILE の歩数は比較できない。** ノードの粒度が違うので「S は 2 倍
速い」は指標の言い換えになりうる。効率の主張は**コンパイル後の R-WHILE 歩数**で
行うこと。上表がその形になっているのはそのためである。

さらに `-steps` は**命令ノード数しか数えず、値の大きさを数えない**。単進 numeral
の `for` は直接でも SI でも n に線形だった（`=? I B` が numeral の長さによらず
1 ノードのため）。二次の仕事は `=?` の中に隠れる。

### `-work` メーター（2026-08-08 実装）

**`./ri -work` は「比較で調べた値のノード数」を積算する。** R-WHILE の原始操作で
費用が定数でないのは比較だけ（cons/hd/tl は 1 ノード、ストアの読み書きはポインタ
1 本）なので、値の大きさが実行時間に現れうる場所はここに限られる。課金する 3 か所は
`=? E F`・`X ^= E` の解除判定（rupdate）・リテラルパターンの照合。nil 判定・
`is_true`・cons/hd/tl・パターン変数の読み書きは O(1) なので課金しない（配列拡張の
添字歩行も未対応。既定オフで、ここで測る成果物はどれも使っていない）。短絡も模型に
入れてある（早い不一致は値がいくら大きくても 1 単位）。実装は `EvalRwhile.eq_work`、
検査は `work-meter` 群 5 件（`for` の二次性を実際に走らせて確認する 1 件を含む）。

**判明したこと 1: Jones 最適性の判定は指標で変わる。**（`./measure_proj jones`）

| 被験 | steps 比 | work 比 |
|---|---:|---:|
| `ri_min` swap | **1.12x（負け）** | **0.71x（勝ち）** |
| `ri_min` id | **1.40x（負け）** | **0.86x（勝ち）** |
| `ri_seq` `[swap]` | 0.50x | 0.41x |
| `ri_perm` `[ab;bc;ab]` | 0.69x | 0.35x |

11 例のうち **steps では 2 例が負けていたのに、work では 11 例すべて勝つ**。
残余は命令ノードを余分に払う代わりに、調べる値のノードを減らしている。
主張は work のほうが強い（両指標で述べるべきである）。

**判明したこと 2: 符号化の大きさは、値ノードを数える指標では実行時費用そのもの。**
（`./measure_proj storewalk`。被験は「恒等＋実行されない n 命令」の 2 系列で、
新しい変数を n 個増やす系列と 1 変数を使い回す系列を対照する）

| n | many-vars \|p2d\| | steps | work | one-var \|p2d\| | steps | work |
|---:|---:|---:|---:|---:|---:|---:|
| 0 | 21 | 190 | 722 | 21 | 190 | 722 |
| 20 | 1443 | 605 | 2482 | 535 | 645 | 1673 |
| 80 | 15243 | **605** | **16282** | 1975 | **645** | **3113** |

**steps は両系列とも完全に平ら**（実行されない命令なので当然）で、被験の大きさを
まったく見ていない。**work は両系列で伸び、`|p2d|` に追随する**。many-vars が
超線形なのは符号化自体が超線形だから——**変数番号が単進だから**である。
`ri.rwhile` のストアは固定 300 スロットなので、どちらの系列もストア長は変えていない。
many-vars が変えているのは**最大添字**だけ。

**この 2 つは「符号化を縮める」の評価を変える。** 前節でビット列 numeral を
「符号 0.24x・歩数の代償は未測定」と書いたが、work で見れば**符号長と実行時費用は
同じ方向**であり、対立しない。残る懸念は `-steps`（＝命令数）が増えることだけで、
それは自己解釈器の書き換え量の問題であって指標の対立ではない。

**判明したこと 3: 自己インタプリタで測ると、可逆版の Jones 最適性は work では
成立し、steps では直線プログラムでのみ成立しない。**（2026-08-09、
`./measure_proj jones-self`、テスト群 `jones-self` 7 件＋`jones-self-open` 3 件）

判明したこと 1 の表は**残余 対 インタプリタ**であって、Jones の基準ではない。
Jones の基準（Jones 1988 / JGS 6 章）は**自己インタプリタ** sint に対する

> `[spec](sint, p)` が p 自身と同等以上に速いこと

である。ri_min/ri_seq/ri_perm の対象言語は R-WHILE ではないので「p を直接走らせる」
が定義できず、これまで測れなかった。`ri_fp3.rwhile` は R-WHILE の自己インタプリタで、
その fp1 残余は 2026-08-08 以降正しく走るので、**いま初めて測れる**。

**ただし比較対象は p ではない。** 可逆射影は**プログラム保存**インタプリタを要求する
（`[sint](p2d p, d) = (p2d p . [p](d))`）ので、残余には「元プログラムも出力する」義務が
ある。p はその仕事をしないから、p と比べるのは射影の定義を測っているだけになる。
公平な下敷きは**同じ義務を果たす p の最小の拡張** p⁺ ＝ p の本体＋2 命令
（自分の符号を定数として置き、出力と対にする。`Simp.program_preserving`）であり、
**可逆版の基準は `残余 ≤ p⁺`** となる。p⁺ が正しい下敷きであることは仮定していない：
`[p⁺](d)`・`[ri_fp3](p2d p, d)`・`[残余](d)` の 3 つが同じ値になることと、
p⁺ の構文的逆が往復することをテストで確認している。

| 被験 | ループ p/残余 | \|残余\| | steps 直接 | steps p⁺ | steps SI | steps 残余(素) | steps 残余 | work p⁺ | work SI | work 残余 |
|---|:---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `id` | 0/0 | 53 | 1 | 5 | 89 | 11 | **3** | 23 | 64 | **23** |
| `id2` | 0/0 | 87 | 1 | 5 | 90 | 19 | 7 | 25 | 75 | **25** |
| `id3` | 0/0 | 139 | 1 | 5 | 184 | 27 | 11 | 38 | 150 | **37** |
| `rep` | 0/0 | 87 | 1 | 5 | 90 | 19 | 7 | 25 | 75 | **25** |
| `swap` | 0/0 | 379 | 3 | 7 | 260 | 41 | 25 | 57 | 234 | **57** |
| `sx_splitjoin` | 0/0 | 343 | 3 | 7 | 260 | 41 | 23 | 57 | 234 | **57** |
| `sx_three` | 0/0 | 543 | 3 | 7 | 354 | 51 | 35 | 72 | 311 | **71** |
| `loop_static2` | **1/0** | 121 | 6 | 10 | 1240 | 15 | **1** | 104 | 1164 | **95** |
| `loop_static3` | **1/0** | 131 | 8 | 12 | 1852 | 15 | **1** | 118 | 1723 | **101** |

（残余は `Simp.copyprop_program` 後。「素」は spec_av が出したまま。
「ループ p/残余」は p と残余に含まれる `CLoop` の個数）

- **work では 9 例すべて `残余 ≤ p⁺`**（`id3`・`sx_three` は 1 単位、
  ループ 2 例は 0.91x・0.86x と下回る）。
  **残余は、義務ぶんを超える比較の仕事をしていない**＝可逆版 Jones 最適性が成立。
- **steps では直線プログラムで成立しない**（`id` の 0.6x を除き p⁺ の 1.4〜5.0x）。
  残余は**命令**を余分に払う——インタプリタのデータ受け渡しが顕在化したもの——が、
  **触るデータの量は増やしていない**。判明したこと 1 と同じ指標依存が、
  対象言語のインタプリタではなく自己インタプリタで再現した。
- **ループ被験では steps でも成立する（p⁺ の 0.1x）。** 2026-08-09 に追加した
  `loop_static2`／`loop_static3` は**静的制御のループ**（カウンタ対リテラル）＋
  **動的な本体**で、spec_av がループを完全に展開する（残余の `CLoop` は 0）。
  残余は**1 命令**になり、反復を 2→3→8 と増やしても **steps 残余は 1 のまま**、
  残余の大きさは 121→131→181 と**反復あたり 10 ノード**で線形に伸びるだけ
  （8 反復は `JONES_EXTRA=<名前> ./measure_proj jones-self` で実測。
  work は 131 対 p⁺ 218＝0.60x）。
  つまり **steps の非成立は「特殊化器が総じて劣る」ことではなく、
  段取りの固定費**であり、直線プログラムにはそれを償却する材料が無いだけである。
  残余が入力に依らない一般のプログラムであること（測定に使っていない 3 種類の
  入力で p⁺ と一致し、構文的逆が往復すること）はテストで確認している。
- **解釈のオーバヘッド自体はほぼ消えている**（自己解釈の 1/10〜1/30 の歩数）。
- **コピー伝播が効かせている**（素の残余は 11→3・51→35 のように必ず多い）。
  したがってこの主張は **(spec_av, `Simp.copyprop_program`) の対**についてのもので、
  `./ri -copyprop` がその対を公開している。
- work 側の caveat：p⁺ と残余のどちらも「符号を出力へ運ぶ」ぶんを課金されるので、
  比が 1.00 なのは**義務ぶんが支配的**だからでもある。主張は「残余の**超過**が 0」
  であって「残余がタダ」ではない。

**判明したこと 4: この表に載らないのは「ループ」ではなく「動的制御」である。**
（2026-08-09、テスト群 `jones-self-open` 3 件）
**——この節は診断の記録。壁は同日中に越えた（判明したこと 5）。**

引き継ぎには「ループを含む被験は fp1-via-ri_fp3 が未対応で測れない」とあったが、
測ってみるとそうではない。**ループそのものは載る**（判明したこと 3 の下 2 行）。
載らないのは**実行時データで分岐するプログラム**で、しかも**ループが無くても
載らない**。

- `examples/reverse.rwhile` を ri_fp3 で**自己解釈するのは正しく動く**
  （インタプリタ側の限界ではない）。
- `[spec_av]((ri_fp3.('S.reverse)))` は spec_av 自身の **`'error <= '41`** で落ちる。
  この定数は `examples/spec_av.rwhile` に**1 か所しか無い**——`'lcheck` ハンドラの
  「出口テストが動的」枝である。`'lcheck` は静的展開が積む継続なので、この失敗は
  「ループの展開を始めたあとで出口テストが静的でなくなった」を意味し、
  spec_av にはそこから残余化へ戻る経路が無い。
- **ループを一切持たない** `examples/dyncond3.rwhile`（動的入力で分岐するだけ）も
  **同じ '41 で落ちる**。動的分岐 → `DYNAMICIZE-ALL` がストア全体を動的化 →
  その中には ri_fp3 の**コードスタック `Cd`** が含まれる → ri_fp3 の主ループ
  `from =? Cd' nil loop STEP-FP3 until =? Cd nil` の出口が展開の途中で動的になる。
- ri_fp3 を通さない**直接の fp1**（被験＝reverse そのもの）も同じ '41 で落ちる。
  部分静的入力の静的な先頭を消費したあと `until =? Y nil` が動的になるため。
- 対照：**直接の被験の動的分岐は残余化できる**（`./measure_proj dyncond`、
  `fp_dyncond_bug` の残余は正しい 2 分岐）。経路が無いのは
  **展開を始めたあとに現れる動的テスト**だけである。

原因の分析・却下した修正案・修正の規模見積りは
`FINDINGS_reversible_projections.md`「10. `'error <= '41`」に置いた。

**判明したこと 5: その壁は越えた——投機的展開＋巻き戻し。**
（2026-08-09、テスト群 `dyn-control` 5 件、`./measure_proj dyncontrol`）

`'error <= '41` は spec_av から消えた。`'loop` ハンドラが静的展開を始める前に
**チェックポイント (`Vl`, `RCode`, `TmpCtr`) を `'lcheck` 継続に積み**、
`'lcheck` が出口テストの動的化を検出したら（＝旧 '41 の位置）そこへ巻き戻して
**ループを入口から残余化**する。巻き戻し先が入口でなければならないのは、
可逆ループが入口テストの真・戻り辺での偽を要求するので**途中から再入場できない**
ためである。残余化では先にストアを動的化し、**そのうえで入口テストを再特殊化する**
（定数 true に畳むと戻り辺を取れない＝非可逆な残余になる）。

**直接 fp1（インタプリタ抜き）でデータ依存ループが初めて表に載る**——そして
**work では可逆 Jones 基準を満たす**（`./measure_proj dyncontrol`）:

| 被験 | \|残余\| | ループ p/残余 | steps p⁺ | steps 残余 | work p⁺ | work 残余 | **Jr(work)** |
|---|---:|:---:|---:|---:|---:|---:|---:|
| `reverse` | 125 | 1/1 | 14 | 18 | 107 | **18** | **0.17x** |
| `length`  | 2273 | 2/2 | 33 | 43 | 259 | **37** | **0.14x** |
| `length2` | 2329 | (2)/2 | 41 | 51 | 318 | **40** | **0.13x** |

- 0.13〜0.17x は静的制御のループ被験（0.91x／0.86x）より**さらに良い**。
- **残余はループを保つ**（静的制御の被験は `lp_r=0` まで展開される）。展開しきれ
  ないから残す、という正しい振る舞いが列に出ている。
- steps は 1.24〜1.30x で不成立。判明したこと 3 と同じ**固定の段取りコスト**で、
  直線被験の 1.4〜5.0x よりむしろ小さい。

**ただし ri_fp3 を通すと最適ではない。** `./measure_proj jones-self` の新しい 2 行
（`reverse` |残余| 46003・Jr(wk) 17.17x、`dyncond3` 45899・5.33x）は**正しいが
大きい**。被験の動的制御がインタプリタの主ループを動的化するので、巻き戻して
残余化するとインタプリタの動的な芯がそのまま残る（残余に 13 個のループ）。
すなわち**「越えた」＝「終わる・正しい」であって「Jones 最適になった」ではない**。
既存 9 被験の数値は 1 つも変わっていない。fp2 も成立したまま
（`[comp2](('S.swap)) == B : true`、比 0.963 → 0.967）。

10.4 が推していた「ワークリスト履歴 `Cd'` を `INV-SPEC-STEP-AV` で逆実行して
巻き戻す」案は**使えない**。spec_av は `X ^= X` でゴミを捨てるので**単射ではなく**、
`X ^= X` の逆は nil を nil に写すだけだからである。逆実行が厳密なのは
ゴミを埋め込む `spec_av_rev.rwhile` の側で、採用したチェックポイントは
その埋め込みを必要な場所にだけ行ったものと読める。詳細と測定は
`FINDINGS_reversible_projections.md` 10.6。

## 保つべき性質

S が可逆であり続けるには、コンパイラが反転と可換であること:

```
compile (inv_S c) ≡ inv (compile c)
```

現在の糖衣ではこれを実測で確認している（`local` の逆がまた `local` 形、
`examples/sugar.rwhile` は順逆とも 50 歩）。Agda 側では核が凍結されるので既存の
27 モジュールは無傷のまま、`compile-sound : ⟦compile c⟧ ≡ ⟦c⟧_S` とコスト関係が
**加算的な新規開発**になる。
