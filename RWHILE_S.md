# R-WHILE-S — 表層言語と、R-WHILE への最適化コンパイラ

**R-WHILE-S**（S = Sugar）は、書きやすさと実行効率のための構文を載せた表層言語。
**R-WHILE の核は凍結する。** 自己解釈の容易さは核の小ささそのものであり、解釈
オーバヘッド定数 `a_p` も `proofs/agda/` の 27 モジュールも核の構文に張り付いて
いる。核に構文を足すと `examples/ri.rwhile`・`examples/spec_av.rwhile`・形式化の
三つ全部に波及する。

```
R-WHILE-S  --(脱糖 Desugar.ml)-->  R-WHILE  --(最適化 Optimize.ml)-->  R-WHILE
                                      |
                                      +--> p2d --> ri.rwhile / spec_av.rwhile
                                      +--> proofs/agda/（核のまま）
```

## 「両取り」はどこまで成立するか

コンパイラは構造を消すので、**ノード数を減らす種類の利得は自己解釈の側へ渡らない**。
最適化は 2 種類に分かれ、扱いが違う。

| 最適化 | コンパイル後も残るか | 実測の根拠（2026-08-06、本機） |
|---|---|---|
| **変数の採番順**（パス 1・実装済み） | **残る**（出力プログラムの採番が変わる） | 実例で SI 歩数 2〜10% 減 |
| **スロット共有**（パス 2・実装済み） | **残る**（出力の変数が実際に減る） | `spec` で 155→119 スロット（23%）。1 個 = 20 SI 歩 |
| 死コード除去・ループ不変移動 | **残る** | 古典的最適化はすべてこちら |
| n 項ブロック | **消える**（`;` に戻る） | 直線 n 命令 = `2n−1` 歩、約半分が `;` |
| push/pop プリミティブ | **消える**（`<=` に戻る） | パターン走査 約 53/267 歩 |

上半分は **`ri.rwhile` と `spec_av.rwhile` を無改変のまま**効かせられる。下半分は
「S を直接実行するとき」にだけ効き、自己解釈に効かせるには核を広げるしかない。

## コンパイラのパス

### パス 1: アクセス重みによる採番（実装済み・`src/Optimize.ml`）

`program2data` は変数を **初出順**で 1..n に採番する。自己解釈系のストアはその番号
で索引する連結リストなので、変数 i の参照はセルを i 個たどる（DLOOKUP/DUPDATE）。
**熱い変数を前に置けば、その走査がすべて短くなる。**

重みは古典的な静的ヒューリスティック — 出現 1 回につき 1 点、囲むループごとに
10 倍（繰り返し実行されるのはそこだから）。条件分岐では倍率を変えない。同点は
初出順を保つので決定的。

```bash
./ri -p2d -hot-vars prog.rwhile     # 重み順で採番して符号化
```

**実測（`examples/ri.rwhile` で自己解釈したときの歩数）**

| プログラム | 変数数 | 既定 | `-hot-vars` | 改善 |
|---|---:|---:|---:|---:|
| `reverse` | 3 | 3773 | 3673 | 2% |
| `rle` | 6 | 8030 | 7798 | 2% |
| `length` | 4 | 8761 | 8317 | 5% |
| `minus` | 4 | 5011 | 4731 | 5% |
| `compare` | 8 | 18027 | 16137 | **10%** |

変数が多いほど効く。純粋な改名なので**答は変わらない**（5 例すべてで直接評価と
一致することを確認、`compiler` テスト群で固定）。

**より大きな効果（後から判明）: 符号化サイズが半分になる。** 変数番号は p2d では
**単進 numeral** で書かれるので、変数 i の出現 1 つが O(i) ノードを食う。出現回数の
降順に並べることは「頻度 × 符号長の総和」を最小化することであり、符号長が番号に
単調なので**この並べ方が最適**である。fp2 のコンパイラは符号化を抱えた残余
プログラムなので、一緒に縮む。

| 指標 | 既定 | `-hot-vars` | 削減 |
|---|---:|---:|---:|
| `spec_av` の符号化 | 817701 ノード | 399565 | **51%** |
| comp2 = `[spec_av]((spec_av.ri_min))` | 812515 ノード | 386681 | **52%** |
| comp2 のループ本体内ノード | 1157729 | 353665 | **69%** |

`[comp2](('S.swap)) == B` は成立したまま。`loop_weight` を 1 / 10 / 100 と振っても
符号化サイズは同一だったので、ヒューリスティックは繊細ではない。

**既定は OFF。** fp1/fp2/fp3 の残余と `examples/*.val` は特定の採番と byte 比較
されているため、既定を変えると壊れる。

### パス 2: スロット共有（実装済み・`./ri -p2d -share-slots`）

生存区間が互いに素な変数に**同じストアスロット**を割り当てる（線形走査レジスタ
割り当て）。

**なぜ効くか（実測 2026-08-06）**: 本文の長さを固定して変数の数だけ変えると、
自己解釈のコストは変数数 n について `387 + 20n`。一方、**同じ変数を再利用する
未実行の命令を 70 個足してもコストはゼロ**だった。課金されるのはストアの
**長さ**であってプログラムの大きさではない。

| k | 同じ変数を再利用 | 毎回別の変数 |
|---:|---|---|
| 10 | 変数 4 → 407 | 変数 13 → 587 |
| 40 | 変数 4 → **407** | 変数 43 → 1187 |
| 80 | 変数 4 → **407** | 変数 83 → 1987 |

**健全性は R-WHILE 自身の不変条件から出る。** 全変数は開始時 nil、終了時 nil
（`all_cleared`）で、値が変わるのは自分の出現箇所だけ。ゆえに変数は**最初の出現
より前と最後の出現より後は nil** であり、出現区間が互いに素な 2 変数が同時に
非 nil になることはない。

補正は 2 つ:

- **ループ**: ある反復で設定し次の反復で消す変数は、区間の外でも非 nil。よって
  ループに触れる区間はそのループ全体まで広げる（入れ子のため不動点まで）。
- **条件分岐は補正不要で、しかもここが稼ぎどころ**。両腕は排他なので then 腕
  だけのスクラッチと else 腕だけのスクラッチは同時に生きない。両腕にまたがる
  変数は区間が両方を覆うので自動的に除外される。

区間の集合は区間グラフなので、開始位置順の貪欲彩色が最適（色数＝同時生存数）。

**実測（スロット数）**

| プログラム | 変数数 | スロット | 削減 |
|---|---:|---:|---:|
| `spec` | 155 | 119 | **23%** |
| `spec_av_rev` | 233 | 198 | 15% |
| `spec_av` | 219 | 193 | 11% |
| `ri` / `compare` / `reverse` | — | 同数 | 0% |

分岐主体の特殊化器で効き、ループ主体の小さな例では**正しく**何も共有しない。
スロット 1 個 = 20 SI 歩なので `spec` は 1 実行あたり約 720 歩の節約。さらに
`specsize` がストアを小さく生成できるので fp2/fp3 全体が速くなる。

**パス 1 と合成すること。** 割り当て順にスロット番号を振ると熱い変数が高い番号に
落ちて、スロットが減っているのに**遅くなる**（実測で `minus` が 7%、
`lookup_ppl2015` が 13% 悪化した）。スロットを共有する変数の重み合計で番号を
振り直すと解消し、7 例すべてで既定以上になる。

### パス 2 を特殊化器に食わせてはいけない

上の健全性の議論は**実行**についてのものである。`spec_av` は被験プログラムを実行
せず、注釈付き値を伝播させる。その AV ストアは**変数番号ごとに 1 つ**の束縛時間
情報を持つので、2 変数を併合すると注釈が併合されてしまい、特殊化としては不健全に
なる。

実測:

| | fp1 残余 (swap / id) | fp2 (comp2) |
|---|---|---|
| 既定 | 103 / 63 ノード | 812515 ノード ✓ |
| `-hot-vars` | 103 / 63 | **386681** ✓ |
| `-share-slots` | **91 / 39**（往復も成立） | **失敗**: `Pattern matching failed: '41 and 'error are not equal (in inv_evalPat)` |

fp1 では動くどころか残余が小さくなるが、fp2 で落ちる。**パス 1 だけなら安全で、
しかも利得のほとんどはそちらにある。** `-share-slots` は自己解釈と直接実行の
経路に限って使うこと。

### パス 3 以降（未着手）

死コード除去、ループ不変移動。`local X in C end` は依然 p2d が符号化できない
（`CLocal cannot be translated to data`）。パス 2 が生存区間を自動で求めるので
`local` は必須ではなくなったが、注釈として使えるようにする余地はある。

## ネイティブ S 評価器（未着手）

n 項ブロックと push/pop プリミティブは、脱糖せず `EvalRwhile.evalCom` が直接
実行して初めて効く。核も p2d も触らないので、`./ri` の拡張として独立に足せる。

## コンパイラは導出できる

R-WHILE で書いた R-WHILE-S インタプリタ `rint_S` があれば、コンパイラは第 1
Futamura 射影で得られる — `target = [rspec](rint_S, src)`。2 つ別々に書いて整合性
を保つ必要はない。ただし可逆言語では通常の射影が成立せず `rint_S` も**プログラム
保存**でなければならない（`FUTAMURA.md`）。現状 fp1 が通っているのは `ri_min`
（5 変数）で、完全な S インタプリタへの拡大は別物の規模である。

## 計測の落とし穴

**S の歩数と R-WHILE の歩数は比較できない。** ノードの粒度が違うので「S は 2 倍
速い」は指標の言い換えになりうる。効率の主張は**コンパイル後の R-WHILE 歩数**で
行うこと。上表がその形になっているのはそのためである。

さらに `-steps` は**命令ノード数しか数えず、値の大きさを数えない**。単進 numeral
の `for` は直接でも SI でも n に線形だった（`=? I B` が numeral の長さによらず
1 ノードのため）。二次の仕事は `=?` の中に隠れる。二進数値・配列のような**データ
表現**の改善を評価するには、触れた値のノード数を積算する `-work` が要る。

## 保つべき性質

S が可逆であり続けるには、コンパイラが反転と可換であること:

```
compile (inv_S c) ≡ inv (compile c)
```

現在の糖衣ではこれを実測で確認している（`local` の逆がまた `local` 形、
`examples/sugar.rwhile` は順逆とも 50 歩）。Agda 側では核が凍結されるので既存の
27 モジュールは無傷のまま、`compile-sound : ⟦compile c⟧ ≡ ⟦c⟧_S` とコスト関係が
**加算的な新規開発**になる。
