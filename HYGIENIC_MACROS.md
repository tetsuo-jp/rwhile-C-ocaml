# 衛生的マクロ展開ガイド（`-hygienic-macros`）

R-WHILE のマクロ展開に関する仕様と、衛生化を前提にプログラムを書く際の指針。

## 2つのセマンティクス

| モード | 起動 | 内部ローカルの扱い |
|---|---|---|
| 非衛生（既定） | （フラグなし） | 内部ローカルを **α変換しない**。マクロ間でローカル名を共有して通信できる |
| 衛生（オプトイン） | `./ri -hygienic-macros ...` | 各展開ごとに内部ローカルを `name-N` に **α変換**する |

- **内部ローカル** = マクロ本体に現れる変数のうち、仮引数でも**グローバル**でもないもの。
- 既定（OFF）の挙動は従来と byte 同一。

## グローバル変数ポリシー（衛生モード）

衛生モードでは **トップレベル（main）本体に現れる変数はグローバルとみなし、α変換しない**。
それ以外の非パラメータ変数は真のマクロローカルとして改名される。

これにより、`spec.rwhile` / `ri.rwhile` のストア `Vl` のような「意図的に共有するグローバル状態」を、
全マクロの引数に通さなくても自由参照のまま使える。一方、各マクロ呼び出しごとのスクラッチは隔離される。

- グローバル集合 = `main` 本体の全変数 ∪ {read 変数, write 変数}。`MacroRwhile.expMacProgram` が展開前に計算。
- 注意（命名規律）：マクロ内部のスクラッチがトップレベル変数と**同名**だと、グローバル扱いになり改名されない。
  スクラッチ名はトップレベル変数と衝突させないこと。

## 衛生化を前提に書くための鉄則

**マクロ境界をまたいで共有する値は、グローバルにするか、引数で受け渡す。**

1. **共有スクラッチは引数に昇格する。**
   マクロと、その逆 `INV-` や別マクロとの間で受け渡す変数（前者が残し後者が消費する作業領域）は、
   すべて仮引数にする。暗黙のローカル共有に頼らない。

2. **意図的なグローバル状態はトップレベルに出す。**
   ストアのような共有状態は `main` で使う（=グローバル扱い）か、引数で通す。
   どちらでもよい（このリポジトリでは `Vl` はグローバル、AUX のスクラッチは引数）。

3. **真のローカルだけがローカルでよい。**
   そのマクロ呼び出し内で生成・消費が完結し、開始時 nil・終了時 nil になる変数
   （例：ループ内で `cons U Vl <= Vl; Rev <= cons U Rev` のように毎周設定・消費される `U`）は、
   α変換されても問題ない。判断に迷ったら引数に昇格する方が安全。

4. **`INV-` ペアは引数を完全一致させる。**
   `M(...)` と `INV-M(...)` は同じ実引数で呼ぶ。衛生モードでは呼び出し側のローカルが一貫して改名され
   引数経由で正しく束縛されるので、これで逆変換が成立する。

## 例：捕捉依存 → 衛生クリーンへの書き換え

`AUX` は元々、結果や累積をローカル `Y, T, Vr, JJ` で `LOOKUP`/`INV-AUX` と共有していた。
これらを引数に昇格すると衛生化に対応する（`U` は毎周完結する真のローカルとして温存）：

```
macro LOOKUP(Vl,J,X)
  AUX(Vl,J,Elem,Rest,Rev,Cnt);
  X ^= Elem;
  INV-AUX(Vl,J,Elem,Rest,Rev,Cnt)

macro AUX(Vl,J,Elem,Rest,Rev,Cnt)
  from (=? Cnt nil) loop
    cons U Vl <= Vl;
    Rev <= cons U Rev;
    Cnt <= cons nil Cnt
  until (=? Cnt J);
  cons Elem Rest <= Vl
```

`Vl`（ストア）は引数のまま、または上位ではグローバルとして共有される。

## 中核プログラムの対応状況

`-hygienic-macros` を付けても付けなくても同じ結果になるよう、以下を書き換え済み：

- `examples/spec.rwhile` / `ri.rwhile` / `spec_av.rwhile` — AUX クラスタのスクラッチを引数化。ストア `Vl` はグローバル。
- `examples/lookup.rwhile` / `lookup_ppl2015.rwhile` — 同様にスクラッチを引数化。
- `examples/ri.rwhile` の固定長ストア配列は、衛生展開で増える変数（**実測 49→257**、2026-08-05）を収容できるよう 300 スロットに拡張。**現状は足りている**（`spec_av` 側の 256 スロットは足りない。下記参照）。

検証済み（ON/OFF 同等）：`spec-macros`, `spec-partial`, `spec-av-*`, `av-algebra`, `spec-ext`,
`file-integration`, `rint`（自己解釈・二重自己解釈）, `hygienic-macros`。
`first-projection`（第1二村射影）は ON/OFF 同一に **既存の理由で red**（[[second-futamura-projection-status]] 参照）であり、本変更とは独立。

### 自己解釈の制約（重要）

衛生展開は変数を1呼び出しごとに新規生成するため、展開後の変数数が増える（`ri.rwhile`: **49→257**、実測 2026-08-05）。
自己解釈系 `ri.rwhile` は **固定長のストア配列**で変数を添字管理しており、被解釈プログラムの変数数が
スロット数を超えると添字あふれする。衛生版 `ri.rwhile` を自己解釈する場合は配列を十分大きく取ること
（現在 300）。さらに大きなプログラムを多重自己解釈する場合は拡張が必要。

## 動作確認

```bash
cd src && make
./ri -exp prog.rwhile                          # 展開結果（ON時は name-N 改名が見える）
./ri -hygienic-macros prog.rwhile data.val     # 衛生モードで評価
make run-tests                                 # 通常（OFF）テスト
RWHILE_HYGIENIC=1 ./test-suite                 # 全テストを衛生モードで実行（検証用）
RWHILE_HYGIENIC=1 ./test-suite test <グループ> # グループ単位（下記の既知の問題を回避）
```

### `RWHILE_HYGIENIC=1 ./test-suite` が全体では通らない理由（2026-08-05 実測）

**コアプログラムが不衛生なのではない。** 原因は 2 つとも衛生化の *副作用* で、
どちらもテストの走らせ方の問題である。

#### 原因 1: 特殊化器の AV ストアが静的寸法

`spec_av.rwhile` の `FpN`（AV ストアのスロット数）は被験プログラムの変数数に
合わせて `specsize` が生成する固定長リストで、現在 **256** スロット。
alpha 変換は変数を 4〜5 倍に増やすため、衛生モードでは足りない。

| プログラム | 通常 | 衛生 |
|---|---|---|
| `spec_av` | 219 | **897** |
| `spec_av_rev` | 233 | **1127** |
| `spec` | 155 | 651 |
| `ri` | 49 | 257 |
| `ri_min`（マクロなし） | 5 | 5 |

溢れるとストア走査が末尾を越え、
`Cannot match cons pattern cons U-1352 Vl against non-cons value nil` になる。
`specsize` を `RWHILE_HYGIENIC=1` 対応にしたので、衛生モード用に寸法を出せる:

```bash
RWHILE_HYGIENIC=1 ./specsize -n ../examples/spec_av.rwhile          # => 897
RWHILE_HYGIENIC=1 ./specsize ../examples/spec_av.rwhile \
                             ../examples/spec_av.rwhile > /tmp/sa.rwhile
```

**検証済み**: こうして `FpN` を 905 に拡げると `second-projection` は衛生モードで
**2 件とも通過**（15.95 秒）。つまりこの失敗は寸法の問題であって、
`spec_av` が不衛生だからではない。

#### 原因 2: 自己適用テストが遅くなる

`rint` グループで予算を超えるのは `rint 9`（**二重**自己適用
`[rint]((rint.(rint.(id.'a))))`）**1 件だけ**。通常でも 74.5 秒かかる。
残り 9 件は衛生モードでも 41 ms 以内で通る。

`ri.rwhile` 自体は衛生モードでも**歩数が完全に同一**（小さな被験プログラムで
534 歩）なので、意味論もコストも変わっていない。単に被験プログラムの変数が
49→257 に増えて SI のストア走査が伸びるだけである（`rint 8` は 96 ms→608 ms、
6.3 倍）。

`rint 9` が衛生モードで**完走するかは未確認**。900 秒でも終わらなかったが、この
計測は別の実験と CPU を奪い合った状態だったため上限として扱えない。単独で長時間
予算を与えた再計測が要る。少なくとも「壊れている」証拠はなく、`rint 8` と同型で
被験プログラムが 5 倍大きいだけである。

#### 原因 3: `spec_av_rev` のガベージスタック `GARB` がグローバル判定されない

`reversible-spec` の失敗はストア寸法ではない（被験プログラムは `ri_min` の 5 変数
なので、寸法を 1136 に拡げても **0 秒で同じように失敗する**）。エラーは
`Some variables are not nil.` で、非 nil で残るのは **50 本の `GARB-N` だけ**:

```
GARB-11   := ((nil . (nil . nil)) . nil)
GARB-403  := (('eq . (('var . (nil . nil)) . ('val . 'swap))) . nil)
...
```

`GARB` は `CLEAR` が捨てる値を積む**マクロ横断の共有アキュムレータ**で、最後に
`EMBED-GARB` が残余プログラムの dead 分岐へ埋め込む。ところが `GARB` は
**ファイル中 14 箇所に現れるが `main` 本体には 1 度も現れない**
（`EMBED-GARB(Out)` の "GARB" はマクロ**名**の一部で、変数の出現ではない）。
グローバル集合は「`main` 本体の全変数 ∪ {read, write 変数}」なので `GARB` は
グローバルにならず、呼び出しごとに `GARB-11`, `GARB-403`, ... と改名される。
結果、各マクロが**自分専用のガベージスタック**へ積み、`EMBED-GARB` は自分の
1 本しか回収できず、残り 50 本が非 nil のまま残る。

これはこのガイドの鉄則 1・2（共有スクラッチは引数に昇格するか、グローバルに
出す）にそのまま該当する。**`spec_av_rev.rwhile` は「中核プログラムの対応状況」の
一覧に入っておらず、衛生対応は元々主張されていない**ので、これは破れではなく
未対応である。直すなら選択肢は 2 つ:

- `GARB` を `CLEAR` 系マクロと `EMBED-GARB` の**引数に昇格**する（鉄則どおりだが
  `GARB` に触る 14 箇所すべてに波及する）。
- `main` 本体で `GARB` に言及してグローバル扱いにする（`GARB ^= nil` は XOR の
  恒等なので意味は変わらないが、意図が読み取りにくい）。

#### まとめ

- 「**中核プログラム**は衛生的」という主張は**支持される**（`spec_av_rev` は
  そもそもその一覧に入っていない）。
- 支持されないのは「`RWHILE_HYGIENIC=1 ./test-suite` をそのまま検証に使える」
  という前提のほう。特殊化器のストアを衛生モード用に再生成し、自己適用テストに
  十分な時間予算を与え、`spec_av_rev` の `GARB` を引数化するまでは通らない。
- 当面はグループ単位で実行するのが確実（`sugar` を含む他の全グループは通る）。

参考実装：
- フラグ・グローバルポリシー：`src/MacroRwhile.ml`（`hygienic`/`globals` ref, `expansionSubst`, `expMacProgram`）
- 変数収集：`src/Subst.ml`（`varsCom` / `varsExp` / `varsPat`）
- フラグ配線：`src/Main.ml`（`-hygienic-macros`）
