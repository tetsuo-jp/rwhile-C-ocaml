# 論文骨子 — 可逆版 Jones 最適性と射影の代数（ロードマップ④）

投稿先案: **情報処理学会論文誌（プログラミング PRO）**、和文。
親論文 `2025_Reversible_Projection_IEICE_D`（IEICE Trans. D 英文、大久保氏チェック待ち）が
**留保した部分**が本稿の範囲なので、**親の完成を待たずに独立して書ける**。

## 1 行の主張（軸）

> **プログラム保存の義務が、最適性の基準と射影の代数の両方を決める。**

義務があるから基準は p から p⁺ へ動き（最適性側）、義務をどこに置くかが塔の代数が
生き残るかを決める（代数側）。**同じ 1 つの原因から 2 つの結論**が出る。

## 貢献（査読者に数えてもらう順）

1. **可逆設定で最適性の基準を初めて定式化した**（`残余 ≤ p⁺`）。
   先行研究 3 件はいずれも基準を述べていない（§関連研究、全文検査で確定）
2. **基準の必然性**: 可逆自己インタプリタはプログラム保存なので残余は ⌜p⌝ の出力義務を負う。
   素の p を基準にすると**射影の定義そのものを測ってしまう**
3. **機械検証**（Agda、`--safe`・postulate 0）: p⁺ の可逆性・意味論・古典版⇒可逆版。
   **加えて「p⁺ は義務を果たす最小のプログラム」を反証**（義務は関数を外延的に固定するが
   コストは関数から決まらない）。言えるのは「p の拡張の中での最小性」
4. **コストモデルの形式化と実測の一致**: `work(p⁺) = work(p) + |⌜p⌝| + 1` を証明し、
   **実測 11 行と `refl` で照合**。`-steps` の絶対定数 `+8` と違い work は符号長に比例する
5. **第 4 射影の退化はゴミの置き場所で割れる**: dead 枝埋込では成立、出力の対に持つ設計では
   不成立（反証は対の非巡回性のみから）。**実機でも `[comp3]('S.spec_av)` が `comp3` と
   バイト一致**。系として「dead 枝埋込は実装の工夫ではなく代数を保存する設計だった」
6. **動的制御の壁を投機的展開＋巻き戻しで越えた**。データ依存ループが初めて work 指標で
   基準を満たす（0.13–0.17×）

**反証が 2 つある**のが本稿の強み。「基準を都合よく緩めたのでは」という第一の問いに、
緩みが加法定数に限ることと真の最小性は原理的に言えないことの**両方を機械検証で**答える。

## 節構成（案）

| 節 | 内容 | 材料の所在 |
|---|---|---|
| 1 序論 | 可逆 PE で「良い特殊化器」を測る物差しが無い | — |
| 2 準備 | R-WHILE、可逆 Futamura 射影、JGS の Definition 6.4 | `RELATED_WORK.md` §1.5 |
| 3 なぜ基準を変えるか | プログラム保存の義務、p⁺ の定義、**「緩めた」ではなく「測る量を正した」** | §1.5 の表 |
| 4 機械検証 | p⁺ の可逆性・意味論・古典⇒可逆・**最小性の反証** | `AGDA_CORRESPONDENCE.md` |
| 5 コストモデル | work 計量の形式化、`work(p⁺)=work(p)+\|⌜p⌝\|+1`、実測 11 行との一致 | `RWhileWork*`・`measured-law` |
| 6 射影の代数 | 合成則、塔の n 段崩壊、**第 4 射影の退化がゴミの置き場所で割れる**、実機のバイト一致 | `RWhileFutamuraAlg*`・`RWhileRevProjAlg*` |
| 7 実装と測定 | 投機的展開＋巻き戻し、被験 9 本＋動的制御 3 本の表 | `measure_proj jones-self`/`dyncontrol` |
| 8 関連研究 | **先行研究 3 件は基準を述べていない**（全文検査の表） | `RELATED_WORK.md` §1.6 |
| 9 限界と今後 | 残余側は測定のみ／抽象層と具象層の橋は部分的／`ri_fp3` 経由の肥大 | 下記 |

## 正直に書く限界（隠すと査読で落ちる）

- **証明したのは基準（p⁺）の側で、残余の work は測定値**。「残余の最適性を証明した」とは書けない
- **抽象層とタイムド核の橋は部分的**（`WorkModel` が未接続。半日規模）
- **`ri_fp3` 経由の残余は正しいが巨大**（17×）。「壁を越えた」＝停止して正しい、であって最適ではない
- 課金③（リテラルパターン）は work のコストモデルに未反映
- 第 4 射影の `fp4` 自体は 1〜3 行で出る**自明な定理**。非自明なのは
  `Embed.def-spec`・`fp4-fails`・`tower-carries` の 3 つ（**先に自分で言う**）

## 先生に用意していただくもの

1. **Overleaf プロジェクト（空）とその git URL**。既存の規約に合わせる:
   `~/dev/overleaf/<名前>/` に clone し、`origin = https://git.overleaf.com/<id>`
2. **投稿先の最終決定**（PRO か IEICE D 和文か）。テンプレートが変わる
3. **親論文との調整方針**（未発表の親を引用できないので、独立して書くか
   「同時進行のコンパニオン」として扱うか）
4. **著者の並び**

1 をいただければ、`~/dev/overleaf/2025_gen_janus_prog_ipsj_pro` の構成
（`ipsj.cls`・`ipsjunsrt.bst`・`latexmkrc`・`../BibTeX/` への symlink・`.gitignore`）を
写して初期化し、§2・§3 から `scidraft` の段落単位 HITL で書き始める。
